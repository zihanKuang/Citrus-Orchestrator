# MCP Streamable HTTP auth token. Apply before the Deployment.
# kubectl apply -f infra/manifests/mcp-server-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: mcp-server-auth
  namespace: citrus
  labels:
    app: mcp-server
type: Opaque
stringData:
  token: "change-me-citrus-mcp"

# MCP server env template (copy to .env — do not commit secrets)

KUBERNETES_NAMESPACE=citrus
PROMETHEUS_URL=http://localhost:9090

# must match agent MCP_AUTH_TOKEN when using streamable-http
MCP_AUTH_TOKEN=change-me-citrus-mcp

MCP_TRANSPORT=stdio
MCP_HOST=0.0.0.0
MCP_PORT=8080

LOG_LEVEL=INFO

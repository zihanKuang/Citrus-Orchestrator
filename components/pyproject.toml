[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "citrus-agent-cli"
version = "0.2.0"
description = "Hand-written ReAct agent CLI (DeepSeek tool calling) for Citrus MCP diagnostics."
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0.0,<2",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]

[project.optional-dependencies]
test = ["pytest>=7.0"]

[tool.setuptools]
packages = ["agent_cli"]
